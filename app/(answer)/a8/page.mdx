import Counter from "@/components/posts/7/Counter";

## 정답: 알파벳 `A`, `B`, `C`, `D`, `D`가 보인다.

<Counter />

### 리스트에서 추가나 삭제 등이 발생해 원소의 순서가 바뀔 경우 `key`에는 `index`를 넣어서는 안된다.

---

설명

---

### 참고자료

-
